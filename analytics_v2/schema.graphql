schema {
    query: Query
}

type Exercise {
    exerciseType: String
    description: String
    totalDuration: Int
}

type Stats {
    username: String
    exercises: [Exercise]
}

type Query {
    weekly_stats(start: String, end: String, username: String): Stats
}